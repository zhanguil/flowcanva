# Furniture AI Studio V0.2 baseline

- 日期：2026-09-09
- 基线：当前本地工作区（包含尚未提交的画布改动）
- 统一入口：`http://127.0.0.1:6789`
- 局域网入口：`http://192.168.18.6:6789`

## 已验证

- 画布单元测试：5 个测试文件、14 个测试通过。
- 画布端到端测试：12 个测试通过。
- 画布生产构建：`vue-tsc -b && vite build`，0 error。
- 管理台生产构建：`vue-tsc -b && vite build`，0 error。
- 后端测试：`go test ./...` 通过。
- 本机与局域网统一入口均返回 HTTP 200。
- 单参考图、多参考图、1:1、3:4、图片生成、生成结果再次作为参考图、并发任务、刷新恢复、历史栈与基本画布操作均有自动化覆盖。
- 新建或未保存分辨率的生图节点默认使用 2K。

## 测试工具

- 项目内临时工具目录使用官方 Go 1.25.3 便携工具链；目录已由 `.gitignore` 排除，不进入产品发布包。

## V0.2 开发约束

- 保留现有无限画布与兼容接口。
- 新领域能力以新增模块和兼容迁移实现。
- API Key 只允许保存在服务端。
- 新模块必须先有独立测试，再接入生产生成调用。
